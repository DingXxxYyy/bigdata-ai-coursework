# 2026-09-03

## 新建概念学习资料生成 Skill（项目级）
- 路径：`.workbuddy/skills/concept-learning-material-gen/SKILL.md`（单文件、自包含、无资源目录）。
- 用途：接收任意单一新概念，产出固定 7 章节学习文档（个人解释/核心机制/应用场景/边界辨析/来源链接/学习路径/自测与常见误区）。
- 特性：概念无关；含输入假设处理（受众背景/深度/语言）、资料来源优先级规则、7 步生成工作流、自检清单；要求每个关键论断带真实可复现 URL，无法证实处标注"待核实"。
- YAML frontmatter 含 `name`、`description`、`agent_created: true`。
- 校验：用脚本确认 name/description/agent_created 字段齐全、全部必需章节存在（本机 managed Python 未装 PyYAML，改用 regex 校验）。
- 追加：同一份 SKILL.md 已复制一份到 `.workbuddy/skills/concept-learning-skill/SKILL.md`（用户指定目录，diff 校验内容一致）。
- 最终态（用户后续确认）：`name` 已改为 `concept-learning-skill`（与目录一致），旧目录 `concept-learning-material-gen/` 已整目录删除。现仅存 `.workbuddy/skills/concept-learning-skill/SKILL.md` 一份。

## 调用 concept-learning-skill 生成三份 HTML 学习资料
- 概念：Agent、大模型的上下文(LLM Context)、Skill；分别调用本项目的 concept-learning-skill 7 章节结构产出。
- 存放：`learning-materials/concept-agent.html`、`concept-llm-context.html`、`concept-skill.html`（均含 7 个 section，自包含 CSS，深色hero卡片风格）。
- 信源：Agent 用 Anthropic building-effective-agents / Lilian Weng / IBM / KAIST 等；Context 用 Lost-in-the-Middle(TACL) / IBM / AWS Labs / Anthropic context engineering；Skill 用 Anthropic Agent Skills 官方文档/skill.md。所有来源链接均为本次实际检索所得，可复现。
- 经验：本仓库 skill 触发靠 frontmatter name，已与目录对齐；给 Agent/Skill 类主题做资料时，明确区分 Skill vs MCP vs Tool vs Workflow 易混概念。
