# 2026-09-04

## 补两份说明文件（承接 09-03 的 concept-learning-skill）
- `learning-materials/concept-relationship.md`：Agent·上下文·Skill 三者关系图，用 Mermaid（flowchart + sequenceDiagram + 边界辨析 flowchart）+ 文字说明。放 learning-materials/ 与三份 HTML 同目录。
- 改写根目录 `README.md`：保留原课程用途与作者，新增 Skill 存放路径（`.workbuddy/skills/concept-learning-skill/SKILL.md`）、调用方法（自然语言点名触发）、目录树更新，并在开头加了"作者本人已人工核查"声明段。
- 用户明确选择"拆分放置"与"改写根目录 README"，勿改成在别处新建 README。
- Mermaid fence 已校验（3 个 mermaid 块、总 6 行 fence 偶数平衡）。

## 强化 .gitignore（防敏感信息泄露）
- 覆盖：在原有 Python/大数据/ML 基础上，新增「敏感信息」区块——env 文件(.env/.env.*/.envrc/*.env)、密钥/凭据(*.pem/*.key/*.p12/credentials*)、云厂商(AWS/gcloud/service-account*.json)、前端本地配置等，及工具缓存。
- 踩坑记录：**gitignore 不支持行内/行尾注释**（`#` 必须在行首才是注释），在 `!.env.example` 后加行尾注释会让该行变成字面 pattern 导致取消失效；已改成整行注释。修复后 `git check-ignore` 验证：.env.example 不被忽略、真实敏感 pattern 全部被忽略、既有 tracked 文件均不受影响。
- 检查结果：仓库当前 tracked 仅 .gitignore/README.md/assignments|docs/README.md，无任何已提交的密钥/敏感文件，基线干净。
